<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate renderer: Bootstrap5PaginationRenderer %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate renderer: Bootstrap5PaginationRenderer %>
